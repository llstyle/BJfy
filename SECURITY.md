# 🔒 Безопасность админ-панели Django

## Зачем скрывать админку?

Стандартный путь `/admin/` — это **первая цель для атак**:
- Автоматические боты сканируют `/admin/` на всех сайтах
- Brute-force атаки на пароли администратора
- Попытки эксплуатации уязвимостей Django admin

## ✅ Что мы сделали

### 1. Переменная окружения для URL админки

В `config/urls.py`:
```python
import os

ADMIN_URL = os.environ.get('ADMIN_URL', 'admin/')

urlpatterns = [
    path(ADMIN_URL, admin.site.urls),
    # ...
]
```

### 2. Настройка в .env файле

```env
ADMIN_URL=secret-control-panel-xyz123/
```

Теперь админка доступна по адресу: `https://yourdomain.com/secret-control-panel-xyz123/`

## 🎯 Рекомендации по выбору URL

### ✅ Хорошие варианты:
- `secret-panel-9k2j3h4/`
- `manage-xyz-2024/`
- `control-x7y8z9/`
- `dashboard-a1b2c3/`

### ❌ Плохие варианты:
- `admin/` — слишком очевидно
- `panel/` — часто сканируется
- `backend/` — легко угадать
- `manage/` — популярный выбор
- `control/` — тоже очевидно

### 💡 Советы:
1. Используй случайную строку из букв и цифр
2. Добавь слеш в конце: `secret-xyz/`
3. Длина 15-30 символов
4. Смешивай буквы, цифры, дефисы

## 🛡️ Дополнительные меры безопасности

### 1. Ограничение доступа по IP (Nginx)

Если админка нужна только с определённых IP:

```nginx
location /secret-control-panel-xyz123/ {
    allow 203.0.113.0/24;  # Твой офис
    allow 198.51.100.42;    # Твой домашний IP
    deny all;
    
    proxy_pass http://127.0.0.1:8000;
    # ... остальные настройки
}
```

### 2. Двухфакторная аутентификация

Установи `django-otp`:

```bash
pip install django-otp qrcode
```

### 3. Django Admin Honeypot

Создаёт ловушку на `/admin/` для логирования попыток взлома:

```bash
pip install django-admin-honeypot
```

```python
# settings.py
INSTALLED_APPS = [
    'admin_honeypot',
    # ...
]

# urls.py
urlpatterns = [
    path('admin/', include('admin_honeypot.urls', namespace='admin_honeypot')),
    path(ADMIN_URL, admin.site.urls),
]
```

### 4. Rate Limiting

Используй `django-ratelimit` для ограничения попыток входа:

```bash
pip install django-ratelimit
```

### 5. Сильные пароли

При создании суперпользователя:
- Минимум 16 символов
- Используй менеджер паролей (1Password, Bitwarden)
- Не используй один пароль для всего

### 6. Логирование попыток входа

В `settings_prod.py` уже настроено логирование. Мониторь файл `logs/django.log` на подозрительную активность.

### 7. Отключи DEBUG в продакшене

```python
DEBUG = False  # Никогда не включай в продакшене!
```

При `DEBUG=True` Django показывает полную информацию об ошибках, включая:
- Пути к файлам
- Переменные окружения
- Запросы к БД
- Настройки приложения

## 📊 Мониторинг

### Sentry для отслеживания атак

```bash
pip install sentry-sdk
```

```python
# settings_prod.py
import sentry_sdk

sentry_sdk.init(
    dsn=os.environ.get('SENTRY_DSN'),
    traces_sample_rate=1.0,
)
```

### Fail2Ban для блокировки IP

Автоматически блокирует IP после множественных неудачных попыток входа.

## 🔍 Проверка безопасности

```bash
# Django security check
python manage.py check --deploy

# Должен показать все проблемы безопасности
```

## ⚡ Быстрая настройка

1. Сгенерируй случайную строку для админки:
```bash
python -c "import secrets; print(secrets.token_urlsafe(16))"
```

2. Добавь в `.env`:
```env
ADMIN_URL=полученная-строка/
```

3. Перезапусти сервис:
```bash
sudo systemctl restart bjfy
```

4. Проверь доступ:
```
https://yourdomain.com/полученная-строка/
```

## 📝 Что запомнить

- ✅ Никогда не используй стандартный `/admin/`
- ✅ Используй секретный URL из переменных окружения
- ✅ Сохрани URL админки в безопасном месте (менеджер паролей)
- ✅ Используй сильные пароли
- ✅ Мониторь логи на подозрительную активность
- ✅ Рассмотри дополнительные меры (2FA, IP whitelist, honeypot)

## 🚨 Что делать если админка скомпрометирована

1. Немедленно измени `ADMIN_URL` в `.env`
2. Перезапусти сервис: `sudo systemctl restart bjfy`
3. Смени пароли всех администраторов
4. Проверь логи на подозрительную активность
5. Проверь БД на несанкционированные изменения
6. Рассмотри добавление 2FA
