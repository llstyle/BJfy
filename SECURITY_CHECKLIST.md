# 📌 Краткая памятка по безопасности

## ⚡ Быстрый чеклист перед деплоем

### 1. Секретные ключи

```bash
# Сгенерируй SECRET_KEY
python generate_secret_key.py

# Сгенерируй секретный URL админки
python generate_admin_url.py
```

### 2. .env файл

```env
SECRET_KEY=сгенерированный-ключ
DEBUG=False
ALLOWED_HOSTS=твой-домен.com
ADMIN_URL=сгенерированный-url/
DB_NAME=bjfy_db
DB_USER=bjfy_user
DB_PASSWORD=надёжный-пароль
```

### 3. Важно!

- ❌ Никогда не используй `/admin/` в продакшене
- ❌ Не коммить `.env` в Git
- ❌ Не включай `DEBUG=True` в продакшене
- ✅ Сохрани admin URL в менеджер паролей
- ✅ Используй сильные пароли (16+ символов)
- ✅ Настрой автобэкапы БД

### 4. После деплоя

```bash
# Проверь безопасность
python manage.py check --deploy

# Проверь что старый /admin/ не работает
curl https://твой-домен.com/admin/  # Должна быть 404

# Проверь что новый URL работает
curl https://твой-домен.com/твой-секретный-url/  # Должна быть страница входа
```

### 5. Доступ к админке

```
https://твой-домен.com/твой-секретный-url/
```

## 🔒 Дополнительная защита (опционально)

1. **Ограничение по IP** (Nginx) - если админка нужна только с твоего IP
2. **Django Admin Honeypot** - ловушка на `/admin/` для отлова ботов
3. **2FA** - двухфакторная аутентификация
4. **Rate Limiting** - ограничение попыток входа

Подробнее: `SECURITY.md`

## 📚 Документация

- `README_DEPLOY.md` - быстрый старт
- `DEPLOYMENT_CHECKLIST.md` - полный чеклист
- `SECURITY.md` - подробно о безопасности админки
